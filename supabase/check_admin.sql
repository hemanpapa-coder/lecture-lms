-- ============================================================
-- 관리자 계정 복구 및 RLS 정책 수정 스크립트
-- ============================================================

-- Step 1: auth.users에서 현재 가입된 모든 사용자의 정보를 확인합니다
-- 이 SELECT 결과로 교수님의 정확한 이메일을 확인하세요!
SELECT id, email, created_at FROM auth.users;
